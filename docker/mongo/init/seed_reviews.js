// Placeholder init script for Mongo (can create indexes)
// This runs only if you wire it in entrypoint (not by default for official image)
// You can later use a custom init process via mounting to /docker-entrypoint-initdb.d
